apiVersion:  v1
kind:  Namespace
metadata:  
   name: dev

---
apiVersion:  v1
kind: pod
metadata: 
       name: nginx-pod
       namespace: dev
spec: 
     containers:
     - name:  nginx
       image: nginx
       ports: 
           - containerPort: 80
